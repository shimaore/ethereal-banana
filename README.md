tools/support application (traces in CCNQ4)
meant to be used with spicy-action's admin roles.

Users must have the following roles assigned to them to use these tools:

    [
      "provisioning_reader",
      "cdrs_reader",
      "locations_reader",
      "logging_reader"
    ]
